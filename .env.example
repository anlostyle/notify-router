NH_USER=admin
NH_PASSWORD=replace-with-a-long-random-password
SESSION_SECRET=replace-with-openssl-rand-hex-32
NOTIFY_VERSION=0.5.0

# Optional global Bearer token for notification APIs. Leave empty for legacy
# clients that cannot add an Authorization header.
NOTIFY_API_TOKEN=

NOTIFY_BIND=127.0.0.1
NOTIFY_PORT=5401
COOKIE_SECURE=0
ACCESS_LOG=0
TZ=Asia/Shanghai
